{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--disable-extensions",
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/dist/**/*.js",
      ],
      "preLaunchTask": "${defaultBuildTask}"
    }
  ],
  "compounds": []
}
